import { Tab, Tabs } from "react-bootstrap";
const ContentTopNav = () => (
  <Tabs defaultActiveKey="General">
    <Tab eventKey="Scheduled Tournaments" title="Scheduled Tournaments">
      yete
    </Tab>
    <Tab eventKey="te Tournaments" title="Scheduled Tournaments">
      yete
    </Tab>
  </Tabs>
);

export default ContentTopNav;
